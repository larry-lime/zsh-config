bindkey -s '\ef' 'f\n' # find files in select folders
# bindkey -s '\ed' 'fd\n' # find directories in select folders
bindkey -s '\eg' 'fc\n' # find files in cwd
bindkey -s '^h' 'cd ~\n' # go home
bindkey -s '\et' 'tmux\n' # launch tmux
